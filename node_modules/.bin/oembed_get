#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../oembed/bin/oembed_get.js" "$@"
  ret=$?
else 
  node  "$basedir/../oembed/bin/oembed_get.js" "$@"
  ret=$?
fi
exit $ret
